.row
  .col-xs-10
    app-shopping-edit
    hr
    ul(class="list-group")
      a(
        '*ngFor'="let ingredient of (shoppingListState | async).ingredients; let i = index"
        class="list-group-item"
        style="cursor: pointer;"
        '(click)'="onEditItem(i)"
      ) {{ ingredient.name }} ({{ ingredient.amount }})
    //- ng-template(#textInfo) Add ingredient please!

